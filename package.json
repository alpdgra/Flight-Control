{
  "name": "flight-control",
  "version": "1.0.0",
  "description": "A browser remake of the classic line-drawing air traffic control game.",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "build": "node build.js",
    "test": "node --test \"test/**/*.test.js\"",
    "icons": "node tools/make-icons.js",
    "verify": "node tools/verify.js",
    "check": "npm run build && npm test && npm run verify",
    "serve": "http-server . -p 8080 -c-1"
  },
  "license": "MIT"
}
